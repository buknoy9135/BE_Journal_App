<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Journal App" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <!-- ✅ Bootstrap CSS without integrity -->
<link
  href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
  rel="stylesheet">
  <%= stylesheet_link_tag "https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css", rel: "stylesheet", crossorigin: "anonymous" %>


    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <div class="position-fixed top-0 start-50 translate-middle-x mt-2 text-center z-3">
      <% if notice %>
        <div class="px-4 py-2 bg-light border border-success text-success rounded shadow-sm d-inline-block">
          <%= notice %>
        </div>
      <% end %>
      <% if alert %>
        <div class="px-4 py-2 bg-light border border-danger text-danger rounded shadow-sm d-inline-block">
          <%= alert %>
        </div>
      <% end %>
    </div>
    <div class="mt-5 pt-5"><%= yield %></div>

    <!-- ✅ Bootstrap JS without integrity -->
<script
  src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
