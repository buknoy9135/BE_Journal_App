<div class="container mt-5">
  <div class="card border-primary border-3 shadow-sm rounded-4">
    <div class="card-body">
      <h2 class="text-primary mb-4">🆕 New Category</h2>

      <%= render 'form', category: @category %>

      <div class="mt-3">
        <%= link_to "⬅️ Back to Dashboard", categories_path, class: "btn btn-outline-secondary btn-sm" %>
      </div>
    </div>
  </div>
</div>
