<h2>Category Details</h2>

<p>
  <strong>Name:</strong><br>
  <%= @category.category_name %>
</p>

<p>
  <strong>Description:</strong><br>
  <%= @category.description %>
</p>

<h2>All Tasks</h2>

<% if @tasks.any? %>
  <table>
    <tr>
      <th>Task</th>
      <th>Description</th>
      <th>Due Date</th>
      <th>Priority</th>
      <th>Status</th>
      <th>Actions</th>
    </tr>
  <%= render @tasks %>
  </table>
<% else %>
  <p>No task yet for this category</p>
<% end %>

<%= link_to 'Edit', edit_category_path(@category) %> |
<%= link_to "Delete", category_path(@category), data: {
                    turbo_method: :delete,
                    turbo_confirm: "Are you sure?"
                  } %> |
<%= link_to 'Create New Task', new_category_task_path(@category) %> |
<%= link_to 'Back to Dashboard', categories_path %>
